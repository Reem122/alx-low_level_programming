#include <main.h>

/**
 * main -Entry point
 *
 * Description: task 0 - Functions, nested loops - alx
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int n;
	char word[8] = "_putchar";
	for(n = 0; n < 8; n++)
	{
		putchar(word[i]);
	}
	putchar("\n");
	return (0);
}
