#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: task 6 - Variables, if, else, while - alx
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int n;

	for (n = 1; n <= 9; n++)
	{
		putchar((char)n);
	}
	putchar('\n');
	return (0);
}
