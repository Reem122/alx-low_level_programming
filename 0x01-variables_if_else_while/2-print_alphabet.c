#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: task 2 - Variables, if, else, while - alx
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;
	
	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	
	putchar('\n');
	return (0);
}
