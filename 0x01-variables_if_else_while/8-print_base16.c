#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: task 2 - Variables, if, else, while - alx
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int c;

	for (c = 48; c <= 57; c++)
	{
		putchar(c);
	}
	for (c = 65; c <= 70; c++)
	{
		putchar(c);
	}

	putchar('\n');
	return (0);
}
