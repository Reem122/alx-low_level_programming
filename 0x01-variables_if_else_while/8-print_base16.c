#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: task 8 - Variables, if, else, while - alx
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int c;

	for (c = 48; c <= 57; c++)
	{
		putchar(c);
	}
	for (c = 97; c <= 102; c++)
	{
		putchar(c);
	}

	putchar('\n');
	return (0);
}
