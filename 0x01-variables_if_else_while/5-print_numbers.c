#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: task 5 - Variables, if, else, while - alx
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf(n);
	}
	putchar('\n');
	return (0);
}
